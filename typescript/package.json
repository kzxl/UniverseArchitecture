{
  "name": "universe-demo",
  "version": "4.0.0",
  "description": "Universe Architecture Pattern — TypeScript Demo",
  "main": "src/index.ts",
  "scripts": {
    "start": "npx tsx src/index.ts",
    "build": "tsc",
    "dev": "npx tsx --watch src/index.ts"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "tsx": "^4.7.0"
  }
}